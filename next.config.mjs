/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "americanas.com.br"
        ]
    }
};

export default nextConfig;
